# -*- mode: snippet -*-
# name: mp-meth-kw
# key: mp-meth-kw
# --
/**
 * Python: $1.$2.$3(*args, **kwargs)
 * @param ${4:*args}
 * @param ${5:**kwargs}
 */
STATIC mp_obj_t ${1:`(mpy-mod-name (buffer-file-name))`}_$2_$3(size_t n_args, const mp_obj_t* pos_args, mp_map_t* kw_args){
    $0
}

STATIC mp_obj_t ${1:`(mpy-mod-name (buffer-file-name))`}_$2_$3(size_t n_args, const mp_obj_t* pos_args, mp_map_t* kw_args);
STATIC MP_DEFINE_CONST_FUN_OBJ_KW($1_$2_$3_fun_obj, ${4:nargs-min}, $1_$2_$3);
